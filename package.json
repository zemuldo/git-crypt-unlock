{
  "name": "git-cryp-unlock",
  "version": "2.0",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.0.0",
    "@actions/exec": "^1.0.0",
    "semver": "^6.3.0"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.20.5"
  },
  "scripts": {
    "package": "ncc build src/build.js -o dist/build && ncc build src/run.js -o dist/run"
  }
}
